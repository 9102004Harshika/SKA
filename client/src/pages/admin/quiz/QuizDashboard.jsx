import React from "react";

const QuizDashboard = () => {
  return (
    <div className="text-secondary">
      <h1 className="text-2xl font-semibold mb-4">Quiz Dashboard</h1>
      <p className="text-base">Manage quizzes, results, and questions. 📝</p>
      {/* Add quiz stats or recent activity here */}
    </div>
  );
};

export default QuizDashboard;
